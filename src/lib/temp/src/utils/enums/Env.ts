enum Env {
  Test = 'test',
  Prod = 'production',
  Dev = 'development',
  RemoteDev = 'remote-dev',
  RemotePreProd = 'remote-pre-prod',
  RemoteProd = 'remote-prod',
  Local = 'local',
}

export default Env
