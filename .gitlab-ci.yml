image: node:14.5.0
cache:
  paths:
    - node_modules/
include:
  - template: Security/SAST.gitlab-ci.yml
stages:
  - Security scans
  - Node.js image
  - AWS image
sast:
  stage: Security scans
  variables:
    SAST_DEFAULT_ANALYZERS: eslint, nodejs-scan
Bundle:
  only:
    - master
    - '/^milestone#.*$/'
  stage: Node.js image
  artifacts:
    paths:
      - api
  script:
    - yarn install
    - yarn prettier
    - yarn tslint
    - yarn test
    - CI_COMMIT_SHA=$CI_COMMIT_SHA CI_COMMIT_REF_NAME=$CI_COMMIT_REF_NAME yarn build
    - mkdir api
    - cp -r aws/. build api
    - cp ecosystem.config.js api/build
Deploy (master):
  only:
    - master
  stage: AWS image
  environment:
    name: Master
    url: https://ask-the-world-master-1144547669.eu-central-1.elb.amazonaws.com/api
  image: registry.gitlab.com/gitlab-org/cloud-deploy/aws-base:latest
  dependencies:
    - Bundle
  script:
    - cd api
    - tar -cvf api.tar build scripts appspec.yml
    - aws s3 cp api.tar s3://ask-the-world-dev-release/master/
    - deploymentId=`aws deploy create-deployment --application-name ask-the-world-master
      --s3-location bucket=ask-the-world-dev-release,key=master/api.tar,bundleType=tar
      --deployment-group-name ask-the-world-master --file-exists-behavior OVERWRITE --output text`
    - aws deploy wait deployment-successful --deployment-id $deploymentId
Deploy (feature):
  only:
    - '/^milestone#.*$/'
  stage: AWS image
  environment:
    name: Feature
    url: https://ask-the-world-feature-507989894.eu-central-1.elb.amazonaws.com/api
  image: registry.gitlab.com/gitlab-org/cloud-deploy/aws-base:latest
  dependencies:
    - Bundle
  script:
    - cd api
    - tar -cvf api.tar build scripts appspec.yml
    - aws s3 cp api.tar s3://ask-the-world-dev-release/feature/
    - deploymentId=`aws deploy create-deployment --application-name ask-the-world-feature
      --s3-location bucket=ask-the-world-dev-release,key=feature/api.tar,bundleType=tar
      --deployment-group-name ask-the-world-feature --file-exists-behavior OVERWRITE --output text`
    - aws deploy wait deployment-successful --deployment-id $deploymentId
